export default {
  app: {
    // JSON Web Token Expires in given hours
    jwtExpIn: 12,
    // When user requests a password reset, how many hours until reset token is expired
    passResetExpDuration: 2,
  },
};
